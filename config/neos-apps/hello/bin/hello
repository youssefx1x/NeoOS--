#!/usr/bin/env bash
# hello — NeoApps demo application.
#   neos-apps run hello [name]   greet <name> or the current user
set -euo pipefail
name="${1:-${USER:-friend}}"
echo "Hello, $name! Welcome to NeoOS."
